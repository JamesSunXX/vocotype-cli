# Plans

版本迭代计划。每个版本有明确目标和可验收的交付物。

## v0.2 — ASR 质量提升（当前）

**目标**：解决英文技术术语识别不准的问题。

- [x] LLM 后处理：修复 API 格式（Chat Completions），改进 prompt 防跑飞
- [x] 热词配置：config.json 中添加 hotword 支持
- [x] 数据标注工具：Web UI（`make annotate`）支持播放、编辑、标记脏数据
- [x] 数据采集：`--save-dataset` 自动保存音频+文本对
- [ ] 积累 200+ 条标注数据
- [ ] Paraformer SFT 微调（本地 M4 Pro 训练）
- [ ] 替换模型并验证效果

## v0.3 — 稳定性与体验

**目标**：日常使用更顺滑。

- [ ] 录音状态指示（菜单栏图标或声音提示）
- [ ] 转录队列可视化（当前有多少任务在排队）
- [ ] LLM 精炼延迟优化（流式输出或更小模型）
- [ ] 错误恢复：ASR 服务断开后自动重连
- [ ] 配置热重载（修改 config.json 不用重启）

## v0.4 — 扩展能力

**目标**：支持更多场景。

- [ ] 多语言支持（英文、日文 ASR 模型切换）
- [ ] 自定义替换词典（正则/映射表后处理）
- [ ] MCP 集成（语音转文字作为 MCP tool 提供）
- [ ] 插件市场：更多后处理插件（翻译、摘要、格式化）

## 已完成

### v0.1 — 基础功能（fork 自 vocotype-cli）

- [x] 热键录音 → FunASR 本地转文字 → 剪贴板注入
- [x] 火山引擎云端 ASR 后端
- [x] LLM 后处理插件
- [x] 数据集采集插件
- [x] Makefile 构建入口
- [x] Harness 工程结构（AGENTS.md / ARCHITECTURE.md / docs/）
